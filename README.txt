It is a basic 2D tetris game. I have implemented nearly all the basic functions. There are some points:
(1) For row elimination, i don't focus only on the last row, but all rows if they are filled.
(2) The game ended when the inital block already has the collision with the existing blocks.